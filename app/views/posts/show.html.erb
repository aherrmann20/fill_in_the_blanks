<%= render 'site_title' %>

<h1>
	<strong>Title:</strong>
	<%= @post.title %>
</h1>

<h2>
	<strong>Author:</strong>
	<%= @post.bru_name %>
</h2>

<p>
	<strong>Body:</strong>
	<%= @post.body %>
</p>

<%= link_to 'Edit', edit_post_path(@post) %> |
<%= link_to 'Back', posts_path %>